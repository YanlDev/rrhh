# =====================================================================
# Base de datos (Drizzle → Postgres de Supabase, sin Supabase Auth)
# Usar el pooler en producción (puerto 6543, pgbouncer):
#   postgres://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct (para migraciones / scripts locales — puerto 5432):
#   postgres://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres
# =====================================================================
DATABASE_URL=
